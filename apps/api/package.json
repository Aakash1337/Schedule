{
  "name": "@schedule/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "11.3.0",
    "@schedule/application": "workspace:*",
    "@schedule/config": "workspace:*",
    "@schedule/database": "workspace:*",
    "@schedule/domain": "workspace:*",
    "fastify": "5.10.0",
    "jose": "6.2.3",
    "pino-pretty": "13.1.3",
    "zod": "4.4.3"
  }
}
