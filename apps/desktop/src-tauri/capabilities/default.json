{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "Minimum capability set for the Schedule desktop startup shell.",
  "windows": ["main"],
  "permissions": []
}
